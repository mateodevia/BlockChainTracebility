apiVersion: v1
kind: Service
metadata:
  name: ca-org1
  # name: orderer-clusterip
spec:
  selector:
    app: ca-org1
  ports:
  - port: 443
    name: ca-org1
  type: ClusterIP

---
# apiVersion: v1
# kind: Service
# metadata:
#   name: ca-org1-nodeport
# spec:
#   selector:
#     app: ca-org1
#   ports:
#     - name: ca-org1
#       port: 443
#       nodePort: 443
#   type: LoadBalancer


---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: ca-org1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ca-org1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: ca-org1
    spec:
      # subdomain: example
      containers:
        - name: ca-org1
          # image: busybox:1.31.1
          # command: [ "sleep", "3600" ]
          command: ["sh", "-c", "fabric-ca-server start -b admin:adminpw -d"]
          image: hyperledger/fabric-ca:1.4.6
          ports:
            - containerPort: 443
              name: web
          env:
            - name: FABRIC_CA_HOME
              value: /etc/hyperledger/fabric-ca-server
            - name: FABRIC_CA_SERVER_CA_NAME
              value: ca-org1
            - name: FABRIC_CA_SERVER_TLS_ENABLED
              value: "true"
            - name: FABRIC_CA_SERVER_PORT
              value: "443"


          volumeMounts:
          - name: ca-org1
            mountPath: /etc/hyperledger/fabric-ca-server
          - name: volume-server-config
            mountPath: /etc/hyperledger/fabric-ca-server/fabric-ca-server-config.yaml
            subPath: fabric-ca-server-config.yaml

      volumes:
        - name: volume-server-config
          configMap:
            name: ca-org1-server-config
        - name: ca-org1
          persistentVolumeClaim:
            claimName: ca-org1
